export type TableElement = {
        /**
         * Container of the data type which is just a single row from the JSON file which is used as a mock API.
         */
        id: number,
        first_name: string,
        last_name: string,
        email: string,
        gender: string,
        ip_address: string,
        time: string
}